parameters: 
environment: 
  GITHUB_APP_ID: ${GITHUB_APP_ID}
  GITHUB_ORG: ${GITHUB_ORG}
  GITHUB_APP_PRIVATE_KEY_BASE64: ${GITHUB_APP_PRIVATE_KEY_BASE64}
  GITHUB_WEBHOOK_SECRET_HMAC: ${GITHUB_WEBHOOK_SECRET_HMAC}
  GITHUB_USERNAME: ${GITHUB_USERNAME}
  GITHUB_REPO_NAME: ${GITHUB_REPO_NAME}
  GITHUB_APP_NAME: ${GITHUB_APP_NAME}
  
  S3_ACCESS_KEY: ${S3_ACCESS_KEY}
  S3_SECRET_KEY: ${S3_SECRET_KEY}
  S3_BUCKET: ${S3_BUCKET}
  S3_REGION: ${S3_REGION}
  S3_ENDPOINT: ${S3_ENDPOINT}

  SLACK_WEBHOOK_URL: ${SLACK_WEBHOOK_URL}

packages:
  - name: github-sync
    environment: 
    parameters: 
    annotations: 
    functions:
      - name: github-webhook
        binary: false
        main: ""
        runtime: nodejs:18
        web: raw
        webSecure: false
        parameters: 
        environment: 
        annotations: 
        limits: 
          timeout: 900000
          memory: 1024
      - name: batch-sync-overview
        binary: false
        main: ""
        runtime: nodejs:18
        web: raw
        webSecure: false
        parameters: 
        environment: 
        annotations: 
        limits: 
          timeout: 900000
          memory: 1024
